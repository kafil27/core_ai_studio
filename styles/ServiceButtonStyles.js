// styles/ServiceButtonStyles.js

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  serviceButton: {
    width: '40%',
    margin: 10,
    borderRadius: 10,
    overflow: 'hidden',
  },
  gradient: {
    padding: 15,
    borderRadius: 10,
    alignItems: 'center',
  },
  serviceText: {
    color: 'white',
    fontSize: 16,
  },
});
